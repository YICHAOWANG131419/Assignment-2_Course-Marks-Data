# Assignment 2 - Course Marks Data

Yichao Wang 10455639

Development steps (commit history)

Commit 1: Initialise Assignment 2 scaffold (rename source + update Makefile)

- Created the Assignment 2 repository using the Assignment 1 template repository.
- Renamed the main source file from `Assignment-1.cpp` to `Assignment-2.cpp`.
- Updated the `Makefile` to compile `Assignment-2.cpp` by replacing references to `assignment-1.cpp` with `Assignment-2.cpp`.
